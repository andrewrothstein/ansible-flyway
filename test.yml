---
- hosts: all
  roles:
    - role: '{{ playbook_dir }}'
  tasks:
    - command: /usr/local/flyway/flyway -v
      register: flyway_test_output
    - name: flyway -v
      debug:
        msg: '{{ flyway_test_output.stdout }}'
